import React from 'react'
import ReactDOM from 'react-dom/client'
// NOTE: On GitHub, ensure App.jsx is in 'src/' and use './App.jsx'
// We use '../App.jsx' here to find the file in the editor's root.
import App from '../App.jsx'
// import './index.css' // Uncomment this on GitHub if you have the CSS file

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
